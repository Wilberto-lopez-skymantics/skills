<!-- SDD Workspace Routing -->
You are operating within a Spec-Driven Development ICM Workspace. 
Read `specs/.sdd/WORKSPACE_ROUTING.md` in this project to understand the pipeline stages and routing instructions before taking action.

CRITICAL PIPELINE RULES:
1. **Continuous State Tracking**: You MUST update `specs/.sdd/_config/sdd-state.json` at every stage transition. Do not skip state logging.
2. **Wireframe Sync**: Before writing implementation phases for any visual spec changes, you MUST update and verify the interactive wireframes in `specs/wireframes/` to act as the visual source of truth.
